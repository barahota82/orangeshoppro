.env.php

# Local backup extract for diffing; do not commit
_compare_old_copy/
_compare_zip_old/

# SQL dump: local reference only; migrations sent as snippets for manual apply
orange_db.sql

# IIS: keep real web.config (Plesk paths, httpErrors) on the server only — not in GitHub
web.config

# Local temp/extract artifacts — never commit
.tmp_*
.pl_extract/
.pl_extract.zip
.pl_tmp.zip
ref1.zip
ref2.zip
ref_extract1/
ref_extract2/
*.local.bak
uploads/

# Generated review/audit exports — not part of the site
docs/exports/
*ADMIN_DB_SCHEMA_AUDIT*.md

# Backup/reference snapshots accidentally committed before
*.php.txt
*.sql.txt
*.sql.TXT

# Local agent sync note — keep locally, do not commit
ORANGE_LAST_SYNC_FROM_AGENT.txt

# Branding marketing material — keep locally, not on GitHub (logos kept)
assets/branding/README.txt
assets/branding/orange_advanced/*.txt
assets/branding/orange_advanced/*.pdf
