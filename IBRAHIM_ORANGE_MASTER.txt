IBRAHIM — Orange | مؤشر أرشيف ومرجع تشغيل
============================================
النسخة المعتمدة: جذر المستودع — IBRAHIM_ORANGE_MASTER.txt (مثلاً D:\orange)
آخر تحديث: 2026-04-26 (إلزام الوكلاء: **ORANGE_STOREFRONT_PERFORMANCE_ROLLOUT.txt**؛ **أولوية إنجاز** مرجع الواجهة **`docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt`** عند إتمام عمل يلمس الواجهة/الطلب — `orange-continuity.mdc`؛ §10: س25/س26؛ س13 + بريد التتبع؛ س30 بنود الطلب الجزئي؛ س19 بريد الضيف؛ **س27** `get-order.php` POST فقط + مرجع نصي للمشاركة في `cart.js`؛ **تعريف كود الدولة (قائمة + رقم)** في أرشيف السياسات قسم «صفر» **ومسارات أدمن الهاتف في «تنفيذ» الأرشيف**؛ س7؛ **س9–س11 وس14** لها «تنفيذ» في الأرشيف؛ **س1–س3 وس6 وس12 وس16–س17 وس20–س21 وس23–س24 وس29** لها «تنفيذ» في الأرشيف (مسارات العربة/الطابور/hero/المخزون/المحاسبة عند التسليم)؛ س28؛ س4 (**عروض المجموع + الهدايا + BOGO** بما فيها حزمة شراء `buy_bundle` + **كومبو** في الأدمن والطابور)؛ **س22** تعديل البنود `amend-order-items.php` — كومبو ثم خصم المجموع ثم هدايا/BOGO؛ **س15** دمج هاتف التسجيل: `storefront_phone_merge.php` + `apply-phone-merge.php` + أدمن `storefront_merge_requests`؛ **س18 وس25–س26** لها «تنفيذ» في أرشيف السياسات؛ **قراءة قائمة الاستمرارية كاملة** في `orange-continuity.mdc` (0–8 و5b، بما فيها TECH_REFERENCE وCOMPLETION_ROADMAP) إلزام أرشيفي؛ **إشعار المالك** بانتهاء الجلسة التنفيذية عند العمل التنفيذي)

الغرض
-------
ملف واحد يجمع: أين المرجع، قاعدة البيانات مقابل الكود، سكربتات SQL، وما يحتاج متابعة
يدوية على السيرفر. مفيد للمحادثات مع الوكلاء وللنشر.

**ملف بوابة واحد (أرسل للوكيل اسمه فقط — يجد فيه قائمة كل القراءات الإلزامية): `ORANGE_AGENT_READ_FIRST.txt`** (جذر المستودع).

**كتالوج المتجر (Unified Taxonomy — قرار مالك، تنفيذ متعدّد المراحل؛ أقسام، شجرة، صفات، مقاس، ألوان، أنماط):** المستند التفصيلي **`docs/archive/ORANGE_UNIFIED_TAXONOMY_AND_CATALOG_ERD.txt`**؛ الملخص السياسي **`docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt`** قسم «تاسعاً». عند أي Patch تصنيف قبل التنفيذ — قراءة كاملة.

**رؤية المستقبل — تعدد الدول والقنوات (قرار مالك 2026-05-20؛ تنفيذ جزئي + **بند 13 فصل كامل** 2026-05-20):** **`docs/archive/ORANGE_OWNER_MULTICOUNTRY_VISION.txt`** — اقرأه كاملاً (وبالأخص **§13**) عند نطاق دولة/مخزن/قناة/طلبات/ذمم/GL/فتح سوق/صلاحيات حسب الدولة.

إلزام الوكلاء قبل أي تنفيذ
---------------------------
**الوكيل ملزَم — قبل أي تنفيذ — بالاطلاع على docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt و IBRAHIM_ORANGE_MASTER.txt و .cursor/rules/orange-session-handoff.mdc و docs/archive/ORANGE_STOREFRONT_PERFORMANCE_ROLLOUT.txt؛ وعند نطاق مخطط/ترحيل scripts/orange_db.sql محلياً إن وُجد (§2أ — الأجزاء ذات الصلة؛ إن غاب الملف لا تفترض المخطط من Git وحدَه)؛ وعند نطاق محاسبة داخلية أو دليل حسابات أو تقارير مالية أو ترحيل GL: docs/archive/ORANGE_ACCOUNTING_MAPPING_AND_REPORT_HANDOFF.txt؛ وعند الحاجة docs/archive/ORANGE_AGENT_QA_REFERENCE.txt؛ ثم قراءة كاملة لقائمة orange-continuity.mdc كما في الأرشيف (ثم باقي الاستمرارية حسب النطاق).**

**تفسير:** «قراءة كاملة» = جميع البنود **0–8** و**5b** في **orange-continuity.mdc** فعلياً. **باقي الاستمرارية حسب النطاق** = مثل **orange-php-utf8-workflow.mdc** و**orange-stack.mdc** وغيرها حسب المهمة. **ROLLOUT** يتضمن **إلزاماً أرشيفياً: عدم المساس بسرعة تحميل الموقع وصفحاته** (قسم «ثانياً»). **الاستثناء:** إن **استثنى المالك ملفاً صراحةً** — وإلا فالالتزام كما سبق.

**تحديث الأرشيف عند قرار سياسة نهائي (قرار مالك 2026-05-20):** أي سياسة يُعدَّل عليها ويُصلَ إلى **قرار نهائي** — **يلزم** تثبيتها في **ملف الأرشيف المناسب** (سؤال/بند + «تنفيذ» إن وُجد كود) **قبل** اعتبار المهمة منتهية؛ لا يكفي التنفيذ أو اتفاق الدردشة. التفاصيل واختيار الملف حسب النطاق في **`.cursor/rules/orange-session-handoff.mdc`** (قسم «تحديث الأرشيف عند قرار سياسة نهائي»).

**إشعار المالك بانتهاء الجلسة:** عند انتهاء **عمل تنفيذي**، **يلزم** الوكيل **إخبار المالك صراحةً** في آخر الرد بجملة **`تم انتهاء الجلسة التنفيذية.`** وتذكير بأن **المحادثة التالية** قد تكون مع **وكيل آخر** دون سياق الدردشة الحالية — **ليعلم المالك**. التفاصيل في **`.cursor/rules/orange-session-handoff.mdc`**.


1) المستودع والكود
-------------------
- المشروع: orangeshoppro (GitHub). آخر دفعة مرفوعة تضمّنت: واجهة المتجر (قنوات، كوكي
  القناة، ترجمة رسائل API)، تمرير lang في الجسم/الرابط، تعزيز أخطاء الشبطة في cart.js،
  صحة health/deploy، وغيرها — راجع سجل commit على main.
- لا تعتمد على نسخة من هذا الملف خارج جذر المشروع؛ المرجع هو الملف في الجذر فقط.


2) قاعدة البيانات — كيف يُبنى المخطط
------------------------------------
أ) مراجعة «الحقيقة» (المخطط الفعلي على السيرفر كما يُصدَّر محلياً):
   - المسار المعتمد دائماً للمراجعة والمقارنة مع الكود:
     D:\orange\scripts\orange_db.sql
   - هذا الملف هو لقطة mysqldump/phpMyAdmin من القاعدة الحقيقية؛ يُحدَّثه المالك عند
     الحاجة (بعد تغييرات على السيرفر) حتى يبقى المرجع مطابقاً للواقع.
   - عند مراجعة المخطط أو الانحرافات: ابدأ من هذا الملف، وليس افتراضاً من الملفات
     الأخرى وحدها.

ب) الملف البرمجي includes/catalog_schema.php — الدالة orange_catalog_ensure_schema()
   - تُستدعى من نقاط كثيرة (متجر، أدمن، طابور الطلبات، …).
   - تضيف/تعدّل جدايلاً وأعمدة (ألوان، مقاسات، طابور الطلبات، حسابات، قيود، عملاء،
     storefront_accounts، …) لكنها لا تنشئ «هيكل المتجر الكامل من الصفر».

ج) الجداول الأساسية للمتجر (مثال: channels, categories, products, orders, …)
   تُفترض موجودة من تثبيت أولي أو من استيراد SQL كامل.

د) قالب قاعدة جديدة تقريباً كاملة في المستودع (للاستيراد على قاعدة فارغة — ليس
   بديلاً عن لقطة الحقيقة أعلاه):
   scripts/mysql-create-orange-database-full.sql
   (يستخدم اسم القاعدة الافتراضي orange_db — يجب مواءمته مع config.php: DB_NAME)

هـ) ملف scripts/orange_db.sql مذكور في .gitignore — dump الحقيقة يبقى محلياً على
   الجهاز وقد لا يظهر في Git؛ المسار D:\orange\scripts\orange_db.sql ثابت كاتفاق مراجعة.


3) جدول product_channels
--------------------------
- الكود يستخدمه: includes/product_channels.php، حذف/إنشاء منتج في الأدمن، إلخ.
- مراجعة الحقيقة على الجهاز: D:\orange\scripts\orange_db.sql — قالب Git:
  mysql-create-orange-database-full.sql.
- تمت إضافة إنشاء الجدول (ومحاولة قيود FK بأسماء orange_fk_pc_* عند غياب أي FK)
  داخل orange_catalog_ensure_schema() في includes/catalog_schema.php — قواعد قديمة
  تُحدَّث تلقائياً عند أول طلب يستدعي ensure_schema (مع وجود جدولي products و channels).


4) سكربتات SQL — ترتيب تنفيذ واحد (إجراء تشغيل)
-----------------------------------------------
قبل أي شيء: mysqldump / نسخة احتياطية كاملة لقاعدة orange_db (أو الاسم في config).

مسار «قاعدة قديمة تريد تقويةها» (لا تستورد الملف الكامل فوق بيانات معقدة دون تخطيط):
  (0) تجربة على نسخة تجريبية إن أمكن.
  (1) scripts/mysql-day1-hardening.sql.txt
  (2) scripts/mysql-day1-hardening-part2.sql
      — إن فشل سطر (مثلاً CREATE INDEX IF NOT EXISTS على إصدار قديم): راجع تعليقات
        آخر الملف؛ قد تحتاج حذف السطر أو إنشاء الفهرس يدوياً.
  (3) scripts/mysql-strong-integrity.sql
      — اقرأ ترويسة الملف؛ إن ظهر Duplicate foreign key: القيد موجود — علّق السطر
        أو احذف القيد القديم بنفس الاسم من information_schema ثم أعد المحاولة.
      — أسماء القيود يجب أن تكون فريدة على مستوى القاعدة؛ إن تعارضت مع جداول أخرى
        استخدم أسماء مثل fk_pc_orange_product (كما في إصلاح product_channels).

مسار «قاعدة فارغة أو إعادة بناء من الصفر»:
  — scripts/mysql-create-orange-database-full.sql (ثم راجع DB_NAME في config).
  — سكربتات التقوية أعلاه اختيارية بعد ذلك إذا احتجت نزامة إضافية على بيانات حقيقية.

ملخص الملفات:
  - mysql-day1-hardening.sql.txt — فهارس/أطوال/تنظيف يتامى/قيود جزئية.
  - mysql-day1-hardening-part2.sql — journal_entries (إجراء يتخطاه إن لم يوجد الجدول) + audit + stock_movements.
  - mysql-strong-integrity.sql — FK وتنظيف أوسع (بعد 1 و 2 عادة).
  - mysql-create-orange-database-full.sql — هيكل تقريباً كامل لقاعدة جديدة.
  - scripts/migrations/*.sql — ترحيلات مرقّمة تُنفَّذ تلقائياً مرة واحدة (جدول orange_schema_migrations)؛
    التفاصيل scripts/migrations/README.txt. الهيكل الأول يبقى من mysql-create؛ الترحيلات للاحق.
  - includes/schema_manager.php — تضمين موحّد يحمّل **catalog_schema.php** (نقطة دخول اختيارية لخطط النشر؛ يستخدمه scripts/run_migrations.php).
  - scripts/run_migrations.php — اختياري بعد النشر: `php scripts/run_migrations.php` يستدعي **orange_run_migrations** (سلسلة ###.sql اختيارية + **orange_catalog_ensure_schema_core**) من CLI؛
    الواجهة تمر عادةً بـ **orange_catalog_ensure_schema** = **orange_schema_check_and_bootstrap** (بوابة + APCu/التقاط اختياري). التفاصيل ROLLOUT.
  - .env.php (اختياري): **ORANGE_STRICT_NUMBERED_SQL_MIGRATIONS**، **ORANGE_SCHEMA_CATCH_BOOTSTRAP_FAILURE**؛ متغير بيئة **ORANGE_SCHEMA_APCU_GATE_SECONDS**؛ متغير بيئة **ORANGE_SCHEMA_OK_FLAG_PATH** (ملف؛ السطر الأول = رقم إصدار المخطط في الكود بعد نشر ناجح) — انظر `.env.example.php` و ROLLOUT «ثامناً».

هذا القسم يغطي «التوثيق/الإجراء الواحد» لترتيب التنفيذ وتجنب تكرار القيود والنسخ الاحتياطي.


5) أطوال حقول categories / subcategories
------------------------------------------
- تم توحيد name_* و slug إلى VARCHAR(191) في scripts/mysql-create-orange-database-full.sql
  وفي includes/catalog_schema.php (ALTER MODIFY عند وجود الجدول) لمواءمة departments
  وباقي الكتالوج utf8mb4.


6) journal_entries مقابل journal_vouchers — قرار مُثبَّت + مخطط جديد نظيف
------------------------------------------------------------------------
قرار المشروع:

أ) المصدر القياسي لقيود الدفتر: journal_vouchers + journal_lines (الترحيل من
   gl_posting، السندات متعددة الأسطر، الطابور orange_gl_pending_movements، …).

ب) قواعد جديدة من scripts/mysql-create-orange-database-full.sql: لا يُنشأ جدول
   journal_entries — القيود تُسجَّل فقط في journal_vouchers / journal_lines.

ج) جدول journal_entries: نموذج تراثي (مدين/دائن في صف واحد). يظل ممكناً في قواعد
   قديمة أو نسخ احتياطية؛ الكود يتعامل معه عبر orange_table_exists (تقارير/فحوص/ترحيل).

د) الترحيل التلقائي: includes/catalog_schema.php — إذا وُجد journal_entries وكان
   journal_lines فارغاً وjournal_entries يحتوي صفوفاً صالحة، يُحوَّل إلى سندات+أسطر
   ثم يُفرَّغ journal_entries (مرة واحدة لكل اتصال ضمن الحارس static).

هـ) صفحة الأدمن «سند قيد»: admin/pages/journal_entries.php (اسم ملف تراثي) تعرض
   وتُدخل عبر journal_vouchers / journal_lines وليس عبر إدراج مباشر في journal_entries.

و) scripts/mysql-day1-hardening-part2.sql — القسم (1) لـ journal_entries يُنفَّذ داخل
   `orange_legacy_journal_entries_hardening`: إن لم يوجد الجدول لا يُنفَّذ UPDATE/ALTER
   (قواعد جديدة من mysql-create). إن وُجد: تصحيح fiscal_year_id اليتيم، إضافة FK إن ناقص، فهرس.

ز) حالة الإغلاق: لا مزيد من «تعليق القسم (1) يدوياً» — السكربت آمن على قاعدة بلا journal_entries.


7) تشغيل إنتاجي — قائمة تحقق سريعة
----------------------------------
- .env.php (غير مرفوع): اتصال DB، بريد (MAIL_FROM وما يلزم لرسائل التأكيد).
- صلاحيات MySQL: إن كان مستخدم التطبيق بدون ALTER، قد تفشل تعديلات catalog_schema
  بصمت (راجع سجلات الخادم / error_log).
- معالج طابور الطلبات: scripts/process_order_intake_queue.php أو جدولة مكافئة على
  السيرفر حتى يكتمل checkout من الطابور.
- ORANGE_API_DEBUG: لا يُترك مفعّلاً على الإنتاج إن كان يكشف تفاصيل للعميل.
- اختبار يدوي: لغات المتجر الأربع، قنوات orange/blue/black، سلة، طلب، تتبع، إلغاء
  حيث يسمح، تسجيل بريد وتأكيد.


8) ملفات SQL أخرى في scripts/
-----------------------------
- mysql-create-orange-database-full.sql — إنشاء قاعدة شبه كاملة.
- mysql-strong-integrity.sql — نزامة قوية بعد التحضير.
- post_deploy_after_upload.sql.TXT — حسب التسمية؛ راجع محتواه قبل التشغيل.
- run_backfill_product_channels.php — CLI لربط كل المنتجات بكل القنوات النشطة
  (يتطلب وجود جدول product_channels).


9) ملاحظة للوكلاء (Cursor)
---------------------------
- قواعد المشروع في .cursor/rules/ عند الحاجة.
- هذا الملف ملخّص تشغيلي/أرشيفي؛ التفاصيل البرمجية الدقيقة في الكود وفي سكربتات SQL.
- نصوص بانر الرئيسية وجمل الهيدر تحت الشعار: شاشة أدمن «بانر الصفحة الرئيسية»
  (`admin/pages/storefront_hero.php`)، جدول `storefront_copy_lines`، منطق العرض
  `includes/storefront_hero.php`؛ مرجع سياسة الواجهة يحدّث حقل س3 في
  `docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt`.
- تنبيه «قارب على النفاذ» (س5): `includes/stock_alerts.php` (`orange_stock_low_alert_threshold`)،
  عرض في `admin/pages/stock.php` ولوحة `dashboard.php`؛ إحصاءات API `admin/api/reports/stats.php`.
- تاب «طلباتي» للضيف (س13): جلسة `orange_sf_guest_recent_phone` في `includes/storefront_account.php`؛
  API `api/orders/list-guest-storefront-orders.php`؛ تعبئة الجلسة من `create-order.php`، `intake-status.php`، `amend-order-items.php`؛
  واجهة `pages/cart.php` + `assets/js/cart.js`.
- طلب جزئي من العربة (س30): عند أكثر من سطر وليس وضع تعديل طلب، اختيار البنود المُرسَلة في `assets/js/cart.js`؛ الباقي يبقى في `localStorage`؛ الإرسال `sendOrderNow` → `create-order.php`؛ وضع التعديل `amend` يُرسل كامل السلة كسابق.
- تعديل بنود الطلب بعد الإرسال (س22): `api/orders/amend-order-items.php` — ترتيب الخصومات كالإنشاء: **كومبو** (`cart_combo_promotions.php`) ثم خصم **مجموع السلة** على الصافي بعد الكومبو ثم **هدايا المجموع وBOGO** (`storefront_checkout_promo_lines.php`)؛ انظر `docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt` س22.
- مناطق التوصيل في الواجهة (س8/س20): عند **صفر** مناطق نشطة في `delivery_areas` — `pages/cart.php` و`pages/register.php` و`pages/track.php` تعرض قائمة معطّلة ورسالة؛ `assets/js/cart.js` (إرسال الطلب) ونماذج التسجيل ترفض الإكمال؛ مفاتيح ترجمة `checkout_delivery_areas_unavailable` و`checkout_delivery_areas_unavailable_note` في `config.php` و`includes/header.php` (`APP_T`).
- بريد تفاصيل الطلب بعد التتبع (س13): `api/orders/email-track-order-summary.php` + عرض في `assets/js/cart.js` (`orangeSendTrackOrderEmailSummary`).
- تقرير مبيعات حسب منطقة التوصيل (س8): `admin/pages/reports.php` — طلبات مكتملة مجمّعة حسب المنطقة.
- بريد الضيف في الطلب (س19): حقل اختياري `#customer_email` في `pages/cart.php`؛ الإرسال من `assets/js/cart.js` (`sendOrderNow`) إلى `api/orders/create-order.php`.
- تحفيز التسجيل عبر العروض (س7): `includes/cart_promotions.php` (`orange_cart_promotion_register_incentive_teaser`)؛ استجابة `api/cart/checkout-preview.php` (`register_promo_teaser`)؛ عرض في `assets/js/cart.js` + `pages/cart.php` (`ORANGE_REGISTER_URL`).
- نطاق العرض مسجّل/الكل (س4): شاشة `admin/pages/cart_promotions.php` — عمود «نطاق العرض»: «جميع الزوّار» أو «مسجّل فقط» (`requires_registered_account`).
- تسعير جزئي لهدايا س4: `cart_bogo_promotions` و`cart_gift_promotions` — أعمدة `gift_unit_charge_kind` / `gift_unit_charge_value`؛ منطق مشترك `includes/cart_promo_gift_charge.php`؛ معاينة `checkout-preview`؛ ترحيل `ORANGE_CATALOG_SCHEMA_PHP_REVISION` 8.
- الطلبات المحجوزة (س28): `admin/pages/reserved_orders.php`.
- دمج هاتف التسجيل (س15 — صفحة register + تأكيد واتس يدوي): `includes/storefront_phone_merge.php`،
  `api/auth/request-email-verify.php`، `api/auth/apply-phone-merge.php`، `admin/pages/storefront_merge_requests.php`،
  جدول `storefront_phone_merge_requests` (يُنشأ عبر `orange_catalog_ensure_schema`).


10) سياسات الواجهة والطلب أونلاين — مرجع إجابات المالك (إلزامي قبل التنفيذ)
---------------------------------------------------------------------------
- **اقرأ قبل أي محادثة/تنفيذ** يخص: البيع على الواجهة، التسجيل، التتبع، الطلب أونلاين،
  الحجز/المخزون، الخصوصية، وارتباط القيد المحاسبي بالتسليم:
  **docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt**
- **معنى «كود الدولة» للوكلاء:** قائمة منسدلة (بادئة دولية واضحة) + حقل رقم منفصل؛
  التعريف الملزم في **قسم «صفر»** داخل الملف أعلاه — لا يُخلط مع تغيير ثيم أو «شكل الموقع»
  العام؛ التنفيذ البرمجي قد يتقدم أو يتأخر عن النص.
- **س25 وس26** (قنوات، روابط، توجيه عند فتح النطاق فقط): مُحدَّدة في قسم **«سادساً»**
  داخل **`docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt`**.
  **مرجع التنفيذ في الكود:** `config.php` — `current_channel_slug()` (ترتيب الأسبقية:
  معامل ‎`?channel=`‎ ثم مقطع مسار قناة من الـ URL ثم كوكي **`orange_sf_ch`**
  (`orange_storefront_read_saved_channel_slug`) ثم القناة الافتراضية)؛ **`index.php`**
  يوجّه إلى الرئيسية بالقناة المحسوبة؛ **`orange_storefront_send_channel_cookie()`**
  يُستدعى من واجهة المتجر (مثل `includes/header.php`) لحفظ آخر قناة نشطة.
- **إشعار نهاية الجلسة للمالك (Cursor):** الوكلاء يلتزمون بـ
  **`.cursor/rules/orange-session-handoff.mdc`** — عند انتهاء عمل تنفيذي يُخبر المالك صراحة
  بـ «تم انتهاء الجلسة التنفيذية» ويتذكّر الوكيل التالي بالقراءات الإلزامية هناك (وملخص **ORANGE_AGENT_QA_REFERENCE** عند الحاجة)
  (انظر قسم **«إلزام الوكلاء قبل أي تنفيذ»** أعلى هذا الملف).


نهاية الملف.
