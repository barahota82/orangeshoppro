ORANGE — خطة مراحل: جدولة عروض السلة + إيقاف تلقائي عند نفاد المخزون
================================================================================
Archive: docs/archive/ORANGE_CART_PROMO_SCHEDULE_ROLLOUT_PHASES.txt
سياسة المالك (س4): docs/archive/ORANGE_STOREFRONT_POLICY_REFERENCE.txt — قرار 2026-06-01
كود مركزي: includes/cart_promo_schedule.php · ترحيل DB: orange_catalog_migrate_cart_promo_schedule_v79
Last updated: 2026-05-20 (خطة التشغيل الاحترافي — مراحل 8–10)

**قرار المالك (2026-05-20):** التنفيذ الاحترافي الكامل = **أكثر من مرحلة واحدة** → يُوثَّق هنا أولاً؛ **لا دفعة كود** لـ 8–10 دفعة واحدة دون توجيه «نفّذ المرحلة X».

**قرارات المالك (ملخّص):**
- تواريخ بداية/نهاية **إلزاميان**؛ التمديد بتعديل نهاية العرض ثم الحفظ.
- عند نفاد المخزون: العرض **يُلغى للعميل بالكامل** (لا رسالة «هدية غير متوفرة»).
- تنبيه الأدمن يميّز: **نفاد مخزون منتجات العرض** (`promo_stock`) و**عدم توفر الهدية** (`gift_stock`).

**للوكلاء:** بعد إغلاق أي مرحلة حدّث جدول «0) ملخص الحالة» و«آخر commit»؛ لا تُغيّر فقرات سياسة س4 إلا بإضافة موجزة أو بند تنفيذ.

================================================================================
0) ملخص الحالة
================================================================================

| المرحلة | العنوان                                      | الحالة   | ملاحظة |
|---------|-----------------------------------------------|----------|--------|
| 1       | ترحيل v79 (أعمدة جدولة + إيقاف تلقائي)       | **مغلق** | backfill سنة للقواعد القديمة |
| 2       | تواريخ إلزامية — أدمن + APIs (1)(2)(3)(4)    | **مغلق** | partials schedule |
| 3       | فلترة المتجر (ضمن المدة + غير موقوف)          | **مغلق** | schedule_sql |
| 4       | إيقاف gift_stock + تنبيه رئيسية               | **مغلق** | هدية + BOGO هدية |
| 5       | إيقاف promo_stock (كومبو + BOGO شراء)        | **مغلق** | أي منتج عرض بلا مخزون |
| 6       | BOGO same_category — إيقاف مخزن              | **مغلق** (ضمن 8) | كل الفئة بلا مخزون |
| 7       | عروض المنتجات (جدول offers)                  | **مغلق** | v80/v81 — commit `b0ef55c` |
| 8       | محرك فحص مخزون موحّد + cron/زر أدمن          | **مغلق** | commit مرحلة 8 |
| 9       | جداول مراقبة + سجل + شاشة صحة العروض        | **مفتوح** | بعد 8 |
| 10      | إعادة تفعيل تلقائي عند رجوع المخزون (اختياري)| **معلق** | قرار مالك: نعم/لا |

**تقدير:** المراحل **1–7** = أساس سياسة (MVP) · **8–9** = تشغيل احترافي · **10** = اختياري.

**آخر commit (مرحلة 8):** `afdba4d` على `main` · (أساس 1–7: `b0ef55c`)

**لماذا ليست «مرحلة واحدة»؟** 8 = منطق + مهام خلفية · 9 = مخطط DB + واجهة مراقبة · 10 = سياسة مختلفة (إعادة التفعيل). دمجها دفعة واحدة يزيد مخاطر الانحراف وصعوبة المراجعة.

================================================================================
1) المرحلة 1 — ترحيل v79
================================================================================
STATUS: **CLOSED**

| #   | المهمة | الحالة |
|-----|--------|--------|
| 1.1 | أعمدة valid_from, valid_to, auto_paused_at, auto_paused_reason على الجداول الأربعة | done |
| 1.2 | backfill قديم: اليوم → +365 يوم | done |
| 1.3 | orange_catalog_migrate_cart_promo_schedule_v79 في catalog_schema.php | done |

================================================================================
2) المرحلة 2 — تواريخ إلزامية (أدمن)
================================================================================
STATUS: **CLOSED**

| #   | المهمة | الحالة |
|-----|--------|--------|
| 2.1 | admin/partials/cart_promo_schedule_fields.inc.php + schedule_js | done |
| 2.2 | cart_promotions · cart_gift · cart_bogo · cart_combo صفحات | done |
| 2.3 | manage.php APIs — تحقق + مسح auto_paused عند الحفظ | done |

================================================================================
3) المرحلة 3 — المتجر
================================================================================
STATUS: **CLOSED**

| #   | المهمة | الحالة |
|-----|--------|--------|
| 3.1 | orange_cart_promo_schedule_sql في استعلامات العروض | done |
| 3.2 | storefront_checkout_promo_lines — تحقق هدية بمعرّفات منتج | done |

================================================================================
4) المرحلة 4 — إيقاف عدم توفر الهدية
================================================================================
STATUS: **CLOSED**

| #   | المهمة | الحالة |
|-----|--------|--------|
| 4.1 | gift_stock عند نفاد **كل** منتجات الهدية (هدية سلة + BOGO) | done |
| 4.2 | بطاقة الرئيسية للأدمن | done |
| 4.3 | ocpStatusLabel — موقوف عدم توفر الهدية | done |

================================================================================
5) المرحلة 5 — إيقاف نفاد مخزون منتجات العرض
================================================================================
STATUS: **CLOSED**

| #   | المهمة | الحالة |
|-----|--------|--------|
| 5.1 | promo_stock — كومبو: أي مكوّن بلا مخزون | done |
| 5.2 | promo_stock — BOGO buy_bundle: أي منتج شراء بلا مخزون | done |
| 5.3 | تنبيه أدمن بنص «نفاد مخزون منتجات العرض» | done |
| 5.4 | orange_cart_promo_product_has_visitor_stock | done |

================================================================================
6) المرحلة 6 — BOGO «نفس الفئة» (مُدمَجة في المرحلة 8)
================================================================================
STATUS: **MERGED → Phase 8** (لا commit منفصل لرقم 6)

**سياسة معتمدة للتنفيذ (بدون انتظار شرح إضافي):**
- **same_category:** إيقاف `promo_stock` فقط إذا **كل** منتجات الفئة النشطة في الكتالوج الموحّد بلا مخزون فعّال للدولة — **ليس** عند نفاد SKU واحد.
- **same_variant:** **لا** إيقاف عالمي (القاعدة لا تربط منتجاً واحداً)؛ يبقى التطبيق عند مطابقة السلة + فحص الهدية `gift_stock` فقط.

| #   | المهمة | الحالة |
|-----|--------|--------|
| 6.1 | منطق «كل الفئة بلا مخزون» داخل المحرك الموحّد | pending → **8.4** |
| 6.2 | توثيق في س4 عند الإغلاق | pending → **8.5** |

================================================================================
7) المرحلة 7 — عروض المنتجات (جدول offers)
================================================================================
STATUS: **CLOSED** (2026-05-20)

| #   | المهمة | الحالة |
|-----|--------|--------|
| 7.1 | ترحيل v80 على `offers` (تواريخ + auto_paused) | done |
| 7.2 | `admin/pages/offers.php` — منتقي نقرتين + فترة إلزامية + تعديل | done |
| 7.3 | `admin/api/offers/save.php` — تحقق تواريخ + مسح إيقاف | done |
| 7.4 | `pages/home.php` — فلترة فترة + إيقاف promo_stock | done |
| 7.5 | تنبيه رئيسية + `includes/product_offers.php` | done |

**ملاحظة:** الخصم = مبلغ يُطرح من `products.price` في تبويب العروض بالرئيسية (ليس سعراً بديلاً في جدول منفصل).

================================================================================
8) المرحلة 8 — محرك فحص مخزون موحّد + تشغيل خلفي (بدون جداول جديدة)
================================================================================
STATUS: **CLOSED** (2026-05-20)

**الهدف:** إيقاف/تنبيه **بلا انتظار زائر**؛ مصدر منطق **واحد** بدل تشتيت `select_rule` / `home` / `combo_best_match`.

| #   | المهمة | ملفات متوقعة |
|-----|--------|----------------|
| 8.1 | `includes/cart_promo_stock_health.php` — `orange_cart_promo_run_stock_health` | **done** |
| 8.2 | تفويض `maybe_pause` / كومبو / رئيسية (`offers` فقط) | **done** |
| 8.3 | `admin/api/cart_promo_stock_health/run.php` | **done** |
| 8.4 | BOGO `same_category`: كل الفئة بلا مخزون → `promo_stock` | **done** |
| 8.5 | س4 — بند تنفيذ المحرك | **done** |
| 8.6 | زر «فحص العروض الآن» — `dashboard.php` | **done** |
| 8.7 | `health.php?rollout=promo-stock-health&key=HEALTH_CHECK_KEY` | **done** |

**معيار إغلاق 8:** فحص يدوي من الأدمن يوقف عرضاً نافد المخزون **دون** فتح الموقع كزائر؛ BOGO فئة يتبع السياسة أعلاه؛ UTF-8 verify؛ commit واحد للمرحلة 8.

**خارج نطاق 8:** جداول سجل · شاشة صحة كاملة · إعادة تفعيل تلقائي (→ 9 و 10).

================================================================================
9) المرحلة 9 — مراقبة وسجل (جداول + شاشة أدمن)
================================================================================
STATUS: **OPEN** — **بعد إغلاق 8.**

| #   | المهمة | ملاحظة |
|-----|--------|--------|
| 9.1 | ترحيل v82: جدول `promo_pause_log` (rule_table, rule_id, reason, paused_at, country_id, meta_json) | تدقيق |
| 9.2 | ترحيل v83: جدول `promo_stock_check` — آخر فحص لكل قاعدة (status ok/warn/paused, checked_at, detail) | لوحة مراقبة |
| 9.3 | المحرك (8) يكتب السجل عند كل إيقاف + يحدّث صف الفحص | ربط |
| 9.4 | `admin/pages/cart_promo_health.php` + `$allowed` في `admin/index.php` — قائمة كل العروض الخمسة بألوان الحالة | واجهة |
| 9.5 | إشعار رئيسية يبقى؛ الشاشة الجديدة = مراقبة **قبل** الإيقاف (تحذير مخزون منخفض على قاعدة) | اختياري ضمن 9 |

**معيار إغلاق 9:** أدمن يرى كل القواعد النشطة وحالة مخزونها آخر فحص؛ كل إيقاف تلقائي له سطر في `promo_pause_log`.

================================================================================
10) المرحلة 10 — إعادة تفعيل تلقائي (معلق — قرار مالك)
================================================================================
STATUS: **ON HOLD**

| خيار | السلوك |
|------|--------|
| **أ (افتراضي حتى قرار)** | إعادة التفعيل **يدوياً** فقط: مخزون + حفظ العرض (يمسح `auto_paused_*` — موجود اليوم) |
| **ب** | عند الفحص الدوري: إن رجع المخزون وما زال ضمن `valid_to` → مسح `auto_paused_*` تلقائياً |

**لا يُنفَّذ 10** حتى يختار المالك **أ** أو **ب** صراحةً.

================================================================================
11) ترتيب التنفيذ للوكلاء
================================================================================

1. مراجعة هذا الملف + س4 قبل أي كود.
2. **مرحلة 8 فقط** عند التوجيه — commit + push + تحديث جدول «0)» و«آخر commit».
3. **مرحلة 9** بعد إغلاق 8 — لا دمج 8+9 في commit واحد إلا بموافقة مالك صريحة.
4. **مرحلة 10** بعد قرار المالك.

================================================================================
12) قالب تقرير للمالك بعد كل دفعة
================================================================================

- **أُغلقت:** (أرقام مراحل)
- **ما زال مفتوحاً:** (أرقام + سبب)
- **على السيرفر:** git pull فقط (ترحيل v79 أوتوماتيك)
- **اختبار يدوي مقترح:** (قائمة قصيرة)
