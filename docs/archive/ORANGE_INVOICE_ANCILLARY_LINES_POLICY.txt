ORANGE — بنود فاتورة إضافية (قائمة محفوظة + دليل) — قرار مالك GAP-ACC-07
Last updated: 2026-05-20
Status: **قرار مالك — اتجاه تنفيذ معتمد** (لم يُنفَّذ بعد؛ GAP-ACC-07 **مفتوح** حتى اكتمال المراحل)

Canonical for: بنود اختيارية على **فاتورة مشتريات** و**فاتورة مبيعات**؛ قيد GL **مركّب** عند الحفظ؛ **قائمة محفوظة** + بحث **الدليل المحاسبي**؛ checkbox **يظهر في الفاتورة المطبوعة**؛ علاقة **إقفال التعديلات**.

اقرأ مع: docs/archive/ORANGE_GL_MOVEMENTS_AND_POSTING_POLICY.txt (§4)، docs/archive/ORANGE_EDIT_LOCK_POLICY.txt، docs/archive/ORANGE_ACCOUNTING_MAPPING_AND_REPORT_HANDOFF.txt، docs/ACCOUNTING_REPORTING_POLICY_V2.md، docs/archive/ORANGE_ADMIN_ACCOUNTING_REPORTS_STATUS.txt (GAP-ACC-07)، **docs/archive/ORANGE_SALES_INVOICE_DOC_UI_HANDOFF.txt** (GAP-SALE-DOC-01 — UI مستند v2 للمبيعات).

================================================================================
0) ملخص قرار المالك (2026-05-20)
================================================================================

ARABIC
- على فواتير **المشتريات** و**المبيعات**: إبقاء مسار «أصناف فقط» كما اليوم، مع **زر «إضافة بند»** (حساب/بند إضافي) لبناء **قيد مركّب** على **نفس المستند** عند الحفظ — **لا** سند يومية منفصل افتراضي لكل بند (§4 GL).
- عند الضغط على «إضافة بند»: **منتقي حساب** (نفس أسلوب كشف الحساب — أوراق ترحيل فقط) مع **مصدرين للبحث**:
  1. **القائمة المحفوظة** — بحث **داخل القائمة فقط** (اختصارات التشغيل المتكررة: شحن، VAT، …).
  2. **الدليل المحاسبي** — بحث **عام** في الشجرة (scoped `country_id`).
- من منتقي **الدليل**: زر **«أضف إلى القائمة»** — يحفظ الحساب في **جدول قائمة دائمة** لإعادة الاستخدام لاحقاً (حسابات جديدة تُضاف أولاً للدليل ثم للقائمة).
- لكل صف بند على الفاتورة: **مبلغ** + **checkbox «يظهر في الفاتورة المطبوعة»** (مثال: شحن مبيعات يرى العميل البند؛ COGS/VAT داخلي قد يبقى مخفياً).
- **حفظ الفاتورة** = ترحيل GL **فوري** (سياسة edit_lock) + أسطر إضافية في **نفس** `journal_vouchers` / `journal_lines` المرتبط بالمستند.
- **التصحيح:** فك قفل → تعديل من شاشة الفاتورة → حفظ in-place — متسق GAP-ACC-03 / ORANGE_EDIT_LOCK_POLICY §0.
- **لا** تُوضَع على الفاتورة: مصروفات تشغيل عامة (إيجار، رواتب، …) — مسارها **سند صرف (PV)** (GAP-ACC-08 تراثي).

ENGLISH
- Optional invoice ancillary lines; compound posting on save; saved picker list + full COA search; show-on-print checkbox; complements static gl_account_settings keys.

================================================================================
1) الفجوة التي يُغلقها (GAP-ACC-07)
================================================================================

ARABIC
- **قبل:** `gl_account_settings` يربط أدوار GL الأساسية فقط؛ لا مفاتيح شحن/VAT؛ القيد غالباً **سطران** (مخزون/إيراد ↔ ذمة/خزينة) على **صافي** المبلغ.
- **بعد التنفيذ:** بنود إضافية **مركّبة** + **خريطة أدوار ديناميكية** (القائمة المحفوظة) + استثناءات من الدليل — **يُغلق GAP-ACC-07** عند اكتمال المرحلة 1–2 أدناه.

================================================================================
2) واجهة المستخدم — سلوك موحّد
================================================================================

| عنصر | سلوك |
|------|------|
| زر «إضافة بند» | يفتح منتقي حساب |
| مصدر البحث | **قائمة محفوظة** \| **الدليل المحاسبي** (تبديل صريح) |
| قائمة محفوظة | `search` ضمن الصفوف النشطة فقط؛ per country |
| الدليل | بحث أوراق ترحيل — مثل `accounts/search-leaves.php` / كشف حساب |
| «أضف إلى القائمة» | من منتقي الدليل فقط؛ بعد اختيار حساب + `line_kind` + `invoice_context` |
| صف البند | `account_id`, `amount`, `show_on_print` (checkbox), `label_ar` اختياري للطباعة |
| طباعة | تعرض البنود حيث `show_on_print = 1` فقط (+ أصناف كما اليوم) |

================================================================================
3) القائمة المحفوظة — نموذج بيانات (مقترح تنفيذ)
================================================================================

جدول مقترح: **`orange_invoice_line_presets`** (اسم تنفيذي — يُثبَّت في `catalog_schema.php`)

| عمود | معنى |
|------|------|
| `id` | PK |
| `country_id` | دولة — إلزامي مع تعدد الدول |
| `account_id` | FK → `accounts` (**ورقة ترحيل** فقط) |
| `label_ar`, `label_en` | اسم يظهر في UI («شحن للعميل») |
| `invoice_context` | `sales` \| `purchase` \| `both` |
| `line_kind` | يحدد **مدين/دائن** في القيد المركّب (§4) |
| `default_show_on_print` | افتراضي checkbox |
| `sort_order`, `is_active` | ترتيب وإخفاء |

**جدول بنود الفاتورة:** **`purchase_invoice_lines`** / **`sales_invoice_ancillary_lines`** (أو جدول موحّد `document_invoice_extra_lines` مع `doc_type`) — يُحدَّد في مرحلة 0 التنفيذ.

================================================================================
4) `line_kind` — اتجاه القيد (إلزامي خلف الكواليس)
================================================================================

ARABIC
- **لا يكفي** اختيار حساب دون قاعدة MD/CR — يُمنع ترك المحاسب يختار اتجاهاً عشوائياً على v1.
- كل preset وكل صف بند يحمل `line_kind`؛ الأمثلة:

| `line_kind` | فاتورة | طرف البند في القيد المركّب | أمثلة حساب |
|-------------|--------|----------------------------|------------|
| `sales_credit_revenue` | مبيعات | **دائن** | إيراد شحن، رسوم، إيرادات أخرى (جذر 4) |
| `sales_debit_contra` | مبيعات | **مدين** | خصم مبيعات (contra إيراد) |
| `sales_credit_liability` | مبيعات | **دائن** | VAT مستحق (جذر 2) |
| `purchase_debit_asset` | مشتريات | **مدين** | مخزون — **للأصناف** (موجود) |
| `purchase_debit_landed` | مشتريات | **مدين** | شحن توريد، جمارك (جذر 5 — `purchase_expenses`) |
| `purchase_debit_vat_input` | مشتريات | **مدين** | VAT مدخلات (جذر 1 أو 2 حسب الدليل) |
| `purchase_credit_contra` | مشتريات | **دائن** | خصم مكتسب |

- **شحن مبيعات للعميل** = **إيراد** (`sales_credit_revenue`) — **ليس** حساب مصروفات (جذر 6).
- **شحن inward مشتريات** = **landed cost** (`purchase_debit_landed`) — جذر **5** لا **6**.

================================================================================
5) بنود مرجعية — للمقارنة مع الدليل (ملحق)
================================================================================

**فاتورة مبيعات — بنود إضافية شائعة (v1):**
S2 شحن/توصيل للعميل · S3 VAT مخرجات · S4 خصم · S5 رسوم خدمة/COD · S6 تغليف/إضافات
(تفصيل جدول المالك في جلسة 2026-05-20 — انظر محادثة GAP-ACC-07)

**فاتورة مشتريات — بنود إضافية شائعة (v1):**
P2 شحن inward · P3 جمارك · P4 VAT مدخلات · P5 خصم مكتسب · P6 منaولة/تأmين

**لا على الفاتورة:** P9/S10 مصروفات تشغيل عامة → **PV**.

================================================================================
6) قيود تقنية ومحاسبية
================================================================================

ARABIC
- **أوراق ترحيل فقط** — `orange_accounts_account_is_posting_leaf`؛ رفض `is_group`.
- **`country_id`:** حسابات وبنود presets scoped؛ `orange_gl_assert_voucher_accounts_country`.
- **التقارير:** تجميع بـ `account_id` / `report_line_id` — لا منطق على **اسم** الحساب (policy v2).
- **edit_lock:** البنود جزء من مستند الفاتورة؛ قفل/فك كGAP-ACC-14.
- **GL فوري:** لا طابور pending للبنود الجديدة (ORANGE_EDIT_LOCK_POLICY).
- **COGS / إيراد أصناف:** يبقى المسار الحالي؛ البنود الإضافية **توسّع** القيد لا تستبدل PIN/SCR/INV.

ENGLISH
- Reuse account search patterns from partner_account_statement / search-leaves.

================================================================================
7) مراحل تنفيذ مقترحة (عند «نفّذ»)
================================================================================

| مرحلة | المحتوى | مخرجات |
|-------|---------|--------|
| **0** | مخطط DB + helpers | **منتهٍ rev 68** — `orange_invoice_line_presets` + `orange_invoice_extra_lines`؛ `includes/invoice_ancillary_lines.php` |
| **1** | مشتريات | `purchases.php` + create/update API؛ منتقي dual-source؛ قيد مركّب؛ طباعة |
| **2** | مبيعات | مسار INV-C / تسليم / manual حسب النطاق؛ checkbox للعميل؛ طباعة |
| **3** | إدارة القائمة | **منتهٍ** — `invoice_line_presets.php` + APIs إدارة/ترتيب |
| **4** | VAT % تلقائي | اختياري — قرار لاحق |

**اختبار:** UTF-8؛ country scope؛ edit_lock؛ لا كسر §13 channels؛ ORANGE_STOREFRONT_PERFORMANCE_ROLLOUT.

**معيار إغلاق GAP-ACC-07:** مرحلة 1 + 2 منتهيتان + توثيق UAT في §8.

================================================================================
8) حالة GAP-ACC-07
================================================================================

| البند | الحالة |
|-------|--------|
| قرار مالك / اتجاه UI | **مغلق 2026-05-20** — هذا الملف |
| تنفيذ كود | **مغلق v1** — مراحل 0–3 (4 اختياري) |
| gl_account_settings ثابت | **يبقى** للأدوار الأساسية؛ القائمة المحفوظة **تكمّل** لا تلغي |

================================================================================
9) عرض البنود الإضافية على الفاتورة + التطابق مع الـ QR (قرار مالك 2026-06-13) — GAP-ACC-07-DISP
================================================================================

قرار المالك بعد نقاش موسّع (2026-06-13) حول **شكل عرض** البنود الإضافية على الفاتورة،
ودخولها ضمن **المبلغ المحصّل من العميل**:

أ) فهم محاسبي مثبَّت (لا تغيير في الترحيل — توثيق فقط):
   - **خصم عمود الأصناف** (`order_items.line_discount`) يُخصَم **مباشرة من الإيراد** (الإيراد يُرحَّل
     صافياً = الكمية×السعر − الخصم عبر `orange_order_item_line_net`) — **ليس** حساب «خصم مسموح» منفصل.
   - **«خصم مسموح»** = بند إضافي `sales_debit_contra` (مدين contra إيراد، حساب واحد داخل الإيرادات،
     يُنقص الإجمالي). **«خصم مكتسب»** مشتريات = `purchase_credit_contra` (دائن، يُنقص التكلفة).
   - **«من يدفع»** محسوم بالاتجاه (دائن=charge يدفعه العميل/يضيف للإجمالي، مدين=خصم يُنقص) — لا تشيك بوكس له.
     أي بند **دائن** (إيراد) **يجب** أن يقابله زيادة تحصيل (الذمة/الخزينة) وإلا لا يتوازن السند.
   - **توصيل يدفعه العميل** → حساب **إيراد** (`sales_credit_revenue`، بند على الفاتورة).
     **توصيل تتحمّله الشركة** → حساب **مصروف**، **خارج** الفاتورة (سند صرف PV) — لا يُضاف كبند.
   - معنى **`show_on_print`** المعتمد: «**بند على فاتورة العميل**» = يظهر للعميل **و**يدخل الإجمالي
     المعروض له (إضافة إن charge، خصم إن مدين). غير المفعّل = داخلي محاسبي لا يظهر ولا يدخل المعروض.

ب) مواصفة العرض المعتمدة (الخيار 2 — مختصر بالأعلى + تفصيل بالأسفل):
   - **رأس الفاتورة (الأعلى):** المسلسل + التاريخ + **الإجمالي النهائي فقط** (متوازن مع صندوق بيانات
     العميل، ثابت الحجم لا يتذبذب بظهور/اختفاء بند الضريبة بين الدول).
   - **صندوق الإجماليات أسفل جدول الأصناف:** التفصيل الكامل بالتسلسل:
        إجمالي الأصناف / − خصم الأصناف / صافي الأصناف /
        + (كل بند charge باسمه: توصيل، رسوم…) / − خصم مسموح (سطر **منفصل** عن خصم الأصناف) /
        + ضريبة القيمة المضافة (**تظهر فقط حيث النسبة>0**؛ الكويت 0% لا بند؛ مصر/غيرها تظهر) /
        = الإجمالي.
   - **الإشارات:** `+` للإضافات و`−` للخصومات (ظاهرة).
   - **الخصمان منفصلان دائماً:** «خصم الأصناف» و«خصم مسموح» سطران مستقلان.
   - **السطر النهائي:** «المبلغ المحصّل» للنقدي / «الإجمالي المستحق» للآجل (حسب حالة المستند).
   - **التطابق:** الإجمالي = صافي الأصناف + الإضافات − الخصومات + الضريبة = ما يدفعه العميل،
     **ونفس القيمة** على الفاتورة المطبوعة وصفحة الـ QR (إغلاق فجوة اختلاف المطبوع عن المدفوع:
     توحيد `show_on_print` مع الإجمالي المعروض).

ج) النطاق المعتمد:
   - **المستندات الخمسة:** INV-C، INV-O، مردود مبيعات، مشتريات، مردود مشتريات.
   - **العرض:** المطبوعة + صفحة الـ QR — **الـ QR للمبيعات الثلاثة فقط** (المشتريات ومردودها مطبوع فقط).

د) خطوات التنفيذ (حالة 2026-06-13):
   1. [ ] صندوق إجماليات سفلي موحّد بالتسلسل أعلاه في الطباعة — `includes/sales_doc_print.php`
          + الصفحات الخمس (`company_sales_invoice.php`، `online_sales_invoice.php`،
          `sales_returns.php`، `purchases.php`، `purchase_returns.php`).
   2. [ ] إبقاء **الإجمالي النهائي فقط** أعلى الفاتورة المطبوعة (نقل التفصيل من رأس البانر للأسفل).
   3. [ ] جلب البنود الإضافية في `includes/public_document_view.php` وعرضها بنفس التسلسل في
          `pages/document.php` (صفحة الـ QR — مبيعات فقط).
   4. [ ] ضمان «المطبوع = المحصّل» وتوحيد منطق `show_on_print` مع الإجمالي المعروض.
   5. [ ] فحص UTF-8 + رفع.
   - **ملاحظة سياق:** صفحة الـ QR حالياً في **وضع معاينة مؤقت** (`?preview=1` + QR placeholder يشير
     لها) — يُزال لاحقاً ليعود التوليد بالتوكن بعد الحفظ (طلب المالك السابق).

================================================================================
Changelog
================================================================================
2026-06-13 — أرشفة قرار مالك GAP-ACC-07-DISP: مواصفة عرض البنود الإضافية (مختصر بالأعلى + تفصيل سفلي بالإشارات، خصمان منفصلان، محصّل/مستحق، الضريبة per-country) على المستندات الخمسة + تطابق الـ QR؛ خطوات التنفيذ §9.
2026-05-20 — أرشفة قرار مالك: بنود فاتورة + قائمة محفوظة + دليل + checkbox طباعة + line_kind؛ GAP-ACC-07 اتجاه تنفيذ.
2026-05-20 — **تنفيذ مرحلة 3:** `invoice_line_presets.php`؛ `presets-admin-list.php`؛ `presets-reorder.php`.
2026-05-20 — **تنفيذ مرحلة 2 (مبيعات):** `manual_order.php` + `create-manual.php` + `order_fulfillment.php`؛ `invoice.php` طباعة؛ `sales-line-kinds.php`.
