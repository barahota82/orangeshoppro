ORANGE — خطة مراحل: إعادة ترقيم id (1، 2، 3… بلا فراغات)
================================================================================
Archive: docs/archive/ORANGE_DB_ID_RENUMBER_ROLLOUT_PHASES.txt
قرار المالك (2026-06-02): **(ب)** كل سجل قديم يُعاد ترقيمه **1، 2، 3…** بلا فراغات، **مع** تحديث كل المفاتيح الأجنبية المرتبطة — **ليس** ضبط `AUTO_INCREMENT` فقط.
مرجع اللقطة الفعلية من السيرفر: **`D:\orange\scripts\orange_db.sql`** (mysqldump حي — يُحدَّثه المالك عند كل مراجعة).
SQL للتنفيذ على السيرفر: **`D:\orange_sql_updates.sql`** (خارج الريبو، آمن لإعادة التشغيل) — انظر `.cursor/rules/orange-php-utf8-workflow.mdc`.
Last updated: 2026-05-20 (إغلاق مرحلة 3 — ترحيل PHP v86)

**للوكلاء:** لا تُنفَّذ كل المراحل دفعة واحدة. بعد إغلاق مرحلة حدّث جدول «0) ملخص الحالة» و«آخر تنفيذ». نسخة احتياطية كاملة **قبل** أي مرحلة.

================================================================================
0) ملخص الحالة
================================================================================

| المرحلة | العنوان                              | الحالة   | ملاحظة |
|---------|---------------------------------------|----------|--------|
| 1       | إعداد / واجهة / دول / سنوات مالية    | **مغلق** | PHP: php_db_id_renumber_phase1_v84 (+ SQL مرجع phase1) |
| 2       | مقاسات وقوالب + أدلة استرشادية       | **مغلق** | PHP: php_db_id_renumber_phase2_v85 · includes/db_id_renumber.php |
| 3       | قواعد GL مساعدة + أنواع قيود        | **مغلق** | PHP: php_db_id_renumber_phase3_v86 |
| 4       | تشغيل ثقيل + دليل حسابات إن لزم     | **مفتوح** | حسب فحص لاحق |

**خارج النطاق (قرار مالك):** خيار **(أ)** — `AUTO_INCREMENT = MAX(id)+1` فقط دون إعادة ترقيم الصفوف القديمة.

**نتيجة مراجعة اللقطة (2026-06-02) — `accounts`:** 250 صفاً، `id` من **1** إلى **250** **بلا فراغات** → **مرحلة 4 لـ `accounts` غالباً غير مطلوبة** ما لم يتغيّر السيرفر بعد اللقطة.

================================================================================
1) قواعد التنفيذ (إلزامية)
================================================================================

1. **ترتيب:** مرحلة 1 → 2 → 3 → 4. لا دمج مراحل في commit/SQL واحد إلا بموافقة مالك صريحة.
2. **لكل مرحلة:** نسخة احتياطية → SQL على نسخة تجريبية إن أمكن → تنفيذ على السيرفر → تحقق (عدد صفوف، FK، عيّنة شاشات أدمن/متجر).
3. **إعادة الترقيم:** جدول mapping قديم→جديد ثم `UPDATE` لكل الجداول التي تشير إلى `id` (بحث في المخطط + `orange_db.sql`).
4. **بعد كل جدول:** `ALTER TABLE … AUTO_INCREMENT = (SELECT MAX(id)+1 …)`.
5. **نشر تلقائي (قرار المالك):** `git pull` → `orange_catalog_ensure_schema()` يشغّل `php_db_id_renumber_phase1_v84` و`php_db_id_renumber_phase2_v85` مرة واحدة.
6. **لا** تُضمَّن dumps كاملة أو أسرار في Git. SQL يدوي اختياري في `orange_sql_updates.sql` على **D:**.
7. **أداة تحليل محلي:** `scripts/analyze_id_gaps.ps1` (قراءة `orange_db.sql` — ليس بديلاً عن فحص السيرفر بعد التنفيذ).

================================================================================
2) المرحلة 1 — إعداد / واجهة / دول
================================================================================
STATUS: **CLOSED** (2026-06-02) — **ترحيل PHP v84** (آلي بعد `git pull`)

**ملف مرجعي:** `scripts/maintenance/db_id_renumber_phase1.sql` · **تنفيذ إنتاج:** `orange_catalog_migrate_db_id_renumber_phase1_v84` في `includes/db_id_renumber.php`.

| جدول | إجراء في اللقطة 2026-06-02 |
|------|---------------------------|
| `analytical_dimension` | **ترقيم:** 9→3، 10→4 (+ `analytical_dimension_value.dimension_id`) |
| `storefront_copy_lines` | **ترقيم:** إغلاق فراغ 6 (7..17 → 6..16) |
| `countries` | **سليم** 1–4 — ضبط `AUTO_INCREMENT` فقط |
| `fiscal_years` | **سليم** صف واحد id=1 |
| `storefront_home_hero` | **سليم** صف واحد id=1 |
| `orange_catalog_schema_checkpoint` | **سليم** صف واحد id=1 |
| `size_family_advisory_library_map` | **سليم** صف واحد id=1 |

**على السيرفر:** نسخة احتياطية (موصى بها) → `git pull` → زيارة أي مسار يشغّل المخطط → تحقق:
`SELECT id FROM analytical_dimension ORDER BY id;`
`SELECT MIN(id), MAX(id), COUNT(*) FROM storefront_copy_lines;`

================================================================================
3) المرحلة 2 — مقاسات وقوالب
================================================================================
STATUS: **CLOSED** (2026-05-20) — **ترحيل PHP v85** (آلي بعد `git pull`)

**ملف مرجعي:** `scripts/maintenance/db_id_renumber_phase2.sql` · **تنفيذ إنتاج:** `orange_catalog_migrate_db_id_renumber_phase2_v85` في `includes/db_id_renumber.php`.

**الهدف:** سلاسل `size_scheme_template_sizes` → `size_family_sizes` → `advisory_sizing_guide_*` → `product_variants` (إن وُجدت بيانات).

| جدول | ملاحظة من اللقطة |
|------|------------------|
| `size_scheme_template_sizes` | **58 فراغاً**؛ يبدأ من **19** لا من 1 |
| `size_family_sizes` | ~768 صفاً؛ فراغ واحد (id 10)؛ يبدأ من 1 |
| `advisory_sizing_guide_cells` | يبدأ من 25 |
| `advisory_sizing_guide_columns` | يبدأ من 7 |
| `advisory_sizing_guide_rows` | يبدأ من 5 |
| `catalog_attribute_options` | يبدأ من 6 |

**معيار إغلاق 2:** إعادة ترقيم مكتملة لسلسلة المقاسات؛ `product_types.default_advisory_sizing_guide_id` و`size_family_sizes.scheme_template_size_id` وغيرها متسقة؛ فحص شاشات المقاسات/الأدلة في الأدمن.

**تحقق بعد النشر:**
`SELECT MIN(id), MAX(id), COUNT(*) FROM size_scheme_template_sizes;`
`SELECT MIN(id), MAX(id), COUNT(*) FROM size_family_sizes;`
`SELECT MIN(id), MAX(id), COUNT(*) FROM advisory_sizing_guide_columns;`
`SELECT MIN(id), MAX(id), COUNT(*) FROM catalog_attribute_options;`
`SELECT 1 FROM orange_schema_migrations WHERE filename IN ('php_db_id_renumber_phase1_v84','php_db_id_renumber_phase2_v85');`

**آخر تنفيذ (كود):** 2026-05-20 — v84/v85 · `ORANGE_CATALOG_SCHEMA_PHP_REVISION` 78.

================================================================================
4) المرحلة 3 — محاسبة (إعداد GL)
================================================================================
STATUS: **OPEN**

| جدول | ملاحظة من اللقطة |
|------|------------------|
| `orange_gl_journal_type_rules` | 18 صفاً؛ من 8→48؛ **23 فراغاً** |
| `journal_types` | 21 صفاً؛ فراغ واحد |
| `orange_gl_setting_alloc` | صف واحد `id=18` |

**معيار إغلاق 3:** قواعد القيود وأنواع القيود متسلسلة من 1؛ إعدادات GL تشير لـ `id` صحيحة؛ لا كسر في `orange_gl_account_settings` / ربط القيود.

================================================================================
5) المرحلة 4 — تشغيل ثقيل + دليل حسابات (عند الحاجة)
================================================================================
STATUS: **OPEN**

**تُنفَّذ فقط** بعد 1–3 وبعد **فحص لحظي** على السيرفر (لقطة `orange_db.sql` محدّثة).

| نطاق | ملاحظة |
|------|--------|
| `accounts` | في لقطة 2026-06-02: **لا فراغات** — **تخطَّ إلا إن ظهرت فجوات جديدة** |
| `products`, `product_variants`, `orders`, `order_items`, `journal_entries`, `journal_lines`, `stock_movements`, `purchases`, … | إن وُجدت بيانات في اللقطة: إعادة ترقيم **مع** تحديث كل FK؛ الأخطر — جدول mapping إلزامي |

**معيار إغلاق 4:** كل جدول متبقٍ مطلوب من المالك — مسلسل كثيف من 1؛ عيّنة طلب/فاتورة/مخزون/قيد تعمل.

================================================================================
6) جداول سليمة في اللقطة (لا مرحلة إلزامية الآن)
================================================================================

أمثلة **بلا فراغات وتبدأ من 1** (مراجعة 2026-06-02): `accounts`, `product_types`, `catalog_categories`, `catalog_subcategories`, `departments`, `channels`, `delivery_areas`, `report_line_master`, `pattern_dictionary`, `color_dictionary`, `admins`, … — **لا تُعاد ترقيمها** دون طلب مالك.

================================================================================
7) ترتيب التنفيذ للوكلاء
================================================================================

1. قراءة هذا الملف + `IBRAHIM_ORANGE_MASTER.txt` (§2أ مخطط `orange_db.sql`).
2. عند التوجيه «نفّذ المرحلة N» — **N فقط**؛ SQL في `D:\orange_sql_updates.sql`.
3. تحديث جدول «0)» أعلاه و«آخر تنفيذ» (تاريخ + ملخص جداول).
4. **لا** إغلاق المشروع كاملاً بعد مرحلة 1 فقط إن بقيت 2–4 مفتوحة.

================================================================================
8) قالب تقرير للمالك بعد كل مرحلة
================================================================================

- **أُغلقت:** (رقم المرحلة)
- **جداول عُولجت:** (قائمة)
- **تُخطّى:** (مع السبب — مثلاً accounts سليم)
- **على السيرفر:** نسخة احتياطية + تنفيذ `orange_sql_updates.sql` + تحقق يدوي
- **ما زال مفتوحاً:** (أرقام المراحل التالية)

— End of rollout phases —
