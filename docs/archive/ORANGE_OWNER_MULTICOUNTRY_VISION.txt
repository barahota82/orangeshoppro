ORANGE — رؤية المالك: تعدد الدول والقنوات والجغرافيا
========================================================
آخر تحديث: 2026-05-20 (+ بند 13؛ **v44–v56**؛ **مرحلة 15 منفّذة**؛ متبقي **16–19**)
الحالة: **قرارات مالك مُثبَّتة** — **بند 13 مراحل 1–15 منفّذة**؛ المتبقي **4 مراحل (16–19)** للفصل الكامل (انظر **§14–§15**).

الغرض
------
مرجع واحد لرؤية المستقبل (متجر متعدد الدول) وترتيب العمل المقترح، حتى لا يُعاد بناء المحافظات/المناطف/القنوات/المنتجات أكثر من مرة.

مصدر القرارات: محادثة مالك–وكيل (2026-05-20).


1) الواجهة — اختيار الدولة
---------------------------
- العميل يختار **الدولة** التي يتعامل معها من الواجهة.
- عند تغيير الدولة يتغيّر ما يخص تلك الدولة: **المنتجات المعروضة**، **مناطق التوصيل** (ومحافظاتها لاحقاً)، **العملة**، وما يُربط بالدولة لاحقاً.
- **يُحفظ** اختيار الدولة و**يُسمح بتغييره** (مثلاً عند السفر).
- **ميزة إضافية (اختيارية لاحقاً):** اقتراح الدولة تلقائياً من **الموقع الجغرافي** للمتصفح — ليس إلزاماً ولا يمنع التغيير اليدوي.

2) المنتجات
-----------
- **كتالوج مختلف لكل دولة** — ليس «نفس المنتج بأسعار متعددة حسب الدولة».

3) العملة
---------
- **لكل دولة عملتها** في العرض والطلبات (عند التوسّع).

4) اللغة
--------
- **غير مرتبطة بالدولة** — العميل يختار لغة الواجهة (عربي / إنجليزي / …) بغض النظر عن الدولة المختارة.

5) ترتيب فتح الأسواق (بعد استقرار الكويت على الهيكل الجديد)
------------------------------------------------------------
1. الكويت (الوضع الحالي — يُرحَّل إلى هيكل «دولة» أولاً)
2. مصر
3. الإمارات
4. السعودية
5. دول أخرى — **الأولوية يحددها المالك لاحقاً**

6) الأدمن والصلاحيات
---------------------
- **أدمن عام:** يرى **كل الدول**.
- **فريق لكل دولة:** صلاحيات **مقيّدة بنطاق دولته** (منتجات، مناطق، طلبات، تقارير، … — التفصيل عند التنفيذ).

7) الدفع
---------
- **حالياً:** نقدي عند الاستلام (COD) — يبقى كما هو.
- **الدفع قبل التسليم / بوابات إلكترونية:** **مؤجّل** حتى اكتمال **الإجراءات القانونية** عند المالك؛ بعدها يُفعَّل مع **طرق دفع وعملة لكل دولة** دون إعادة هيكلة الدول.

8) القنوات (channels) وعلاقتها بالدولة
---------------------------------------
- **كل قناة تابعة لدولة واحدة** — ليست قنوات عالمية مشتركة بين الدول.
- **عدد القنوات لكل دولة مفتوح** — لا حدّ أقصى (ليس مقيداً بقناتين فقط).
- يمكن لنفس الدولة: عدة قنوات **ويب**، وعدة قنوات **واتساب**، وقنوات **أخرى** (مثل ما يعادل قنوات تسويق إضافية اليوم في الكويت) حسب الحاجة.
- **المنتج** يُربط بالقنوات ضمن **نفس الدولة** فقط.
- **المخزون داخل الدولة:** **لا** مخزن منفصل **لكل قناة** — القنوات للعرض والوصول للجمهور فقط (**س6** للكويت أحادية الدولة).
- **المخزون بين الدول:** **فصل كامل** — لكل دولة **مخزن/مخازن** وكمياتها؛ لا يُخصَم مخزون دولة من طلب دولة أخرى. (التفصيل والتنفيذ في **البند 13** — يُلغي صياغة «مخزون موحّد عالمياً على المتغير» في هذا الملف سابقاً.)

9) الجغرافيا — محافظات ومناطق
-------------------------------
- داخل **كل دولة:** **محافظة (أو مستوى إداري مماثل) ← مناطق توصيل تابعة لها**.
- مناطق الكويت الحالية (`delivery_areas`) تُرحَّل تحت **دولة الكويت** + هيكل محافظات — **بعد** تثبيت جدول/مفهوم **الدول** في المخطط (لا بناء محافظات بلا `country_id`).

10) ترتيب العمل المقترح (لتقليل إعادة الشغل)
--------------------------------------------
**المرحلة 0 — توثيق:** هذا الملف + عدم تنفيذ محافظات/منتجات متعددة الدول قبل أساس الدولة.

**المرحلة 1 — أساس الدولة (أول تنفيذ برمجي):** **تنفيذ جزئي**
- جدول `countries` (ورموز/عملات/تفعيل).
- ربط `channels.country_id` + تمييز نوع القناة حيث يلزم (ويب / واتساب / أخرى) **بدون حدّ لعدد القنوات**.
- اختيار الدولة في الواجهة (الكويت الوحيدة النشطة في البداية) + حفظ وتغيير.
- ترحيل قنوات الكويت الحالية تحت دولة الكويت.
- إضافة `country_id` على `delivery_areas` (قبل أو مع محافظات الكويت).

**المرحلة 2 — جغرافيا الكويت:** **تنفيذ جزئي** — محافظات + مناطق + شاشة أدمن محافظات/مناطق؛ سياق `admin_country` على عملاء/موردين/مناطق.

**المرحلة 3 — فصل تشغيلي كامل (بند 13):** مخازن + `country_id` على الطلبات/العملاء/الموردين/المشتريات/المنتجات + محاسبة وذمم scoped بالدولة — **ترتيب التنفيذ البرمجي التفصيلي في البند 13**.

**المرحلة 4 — صلاحيات أدمن:** `admin_users.country_id` (NULL = أدمن عام) + منع تجاوز الدولة.

**المرحلة 5 — تفعيل دول إضافية** بالترتيب في البند (5) أعلاه (مصر ثم الإمارات ثم السعودية …).

**مؤجّل:** دفع إلكتروني (بند 7)؛ كتالوج موحّد ضخم (شجرة/صفات) — انظر ORANGE_UNIFIED_TAXONOMY_AND_CATALOG_ERD.txt ما لم يوجّه المالك خلاف ذلك.

11) علاقة بملفات أرشيف أخرى
----------------------------
- سياسات الواجهة الحالية (س8 مناطق، س25/س26 قنوات، س6 مخزون/قنوات): **ORANGE_STOREFRONT_POLICY_REFERENCE.txt** — تبقى سارية للكويت حتى يُحدَّث «تنفيذ» متعدد الدول.
- أداء الواجهة: **ORANGE_STOREFRONT_PERFORMANCE_ROLLOUT.txt** — أي توسّع دول/قنوات لا يكسر المسار الساخن.
- خارطة المشروع العامة: **ORANGE_PROJECT_COMPLETION_ROADMAP.txt** — مراحل المحاسبة/الكتالوج منفصلة عن هذا الملف.

12) للوكلاء
-----------
- عند نطاق **دولة / قناة لدولة / محافظة / فتح سوق جديد / مخزن / فصل GL:** اقرأ **هذا الملف كاملاً** قبل Patch — **وبالأخص البند 13** عند أي عمل يلمس مخزوناً أو طلباتاً أو ذمماً أو قيوداً متعددة الدول.
- لا تُنفَّذ **محافظات الكويت** وحدها بلا `country_id` إذا كان المالك يسير برؤية هذا الملف.


13) فصل تشغيلي كامل بين الدول (قرار مالك — 2026-05-20)
---------------------------------------------------------
**مصدر القرار:** محادثة مالك–وكيل (2026-05-20) — تأكيد صريح: **فصل كامل** بين الدول (مخزن، عملاء، موردين، مشتريات، مبيعات، حسابات/ذمم، محاسبة GL). **أدمن عام** يدخل أي دولة ويرى **حركاتها المنفصلة** دون خلط.

**نموذج التطبيق (قرار معتمد):**
- **قاعدة بيانات واحدة** — الفصل عبر **`country_id`** (نطاق دولة) على السجلات التشغيلية، **لا** قاعدة منفصلة لكل دولة.
- **كل استعلام/إدراج** في الأدمن والـ API التشغيلي يمر عبر **سياق الدولة** (`orange_admin_context_country_id` / سياق واجهة من القناة).
- **ممنوع** ربط عملية (طلب، شراء، قيد، خصم مخزون) بكيانين من دولتين في نفس الحركة.

**13.1 مخزون**
- جدول **`warehouses`** (تابع لـ `country_id`؛ مخزن افتراضي لكل دولة).
- جدول **`warehouse_variant_stock`** (كمية لكل متغير في مخزن).
- **ترحيل الكويت:** نقل `product_variants.stock_quantity` الحالي إلى مخزن الكويت الافتراضي عند أول ترحيل.
- **المرحلة الانتقالية:** يُسمح مؤقتاً ببقاء `product_variants.stock_quantity` متزامناً مع المخزن الافتراضي للكويت حتى اكتمال ربط كل مسارات البيع/الشراء/التعديل — ثم يصبح مصدر الحقيقة **`warehouse_variant_stock`** داخل نطاق الدولة.
- **القناة** لا تملك مخزناً؛ **الدولة** تملك المخزن.

**13.2 منتجات**
- **`products.country_id`** (إلزامي عند التوسّع) — كتalog مختلف لكل دولة (البند 2).
- **`product_channels`** ضمن **نفس الدولة** فقط.

**13.3 عملاء وموردون وذمم**
- **`customers.country_id`**, **`suppliers.country_id`** — سجل مستقل لكل دولة (كود عميل/تسلسل **لكل دولة**).
- **نفس الشخص** في دولتين = **سجلّان** في `customers` (لا ذمم مشتركة بين الدول).

**13.4 مبيعات ومشتريات**
- **`orders.country_id`**, **`purchases.country_id`**, **`stock_movements.country_id`** (+ `warehouse_id` حيث ينطبق).
- أرقام/تسلسلات (طلبات، فواتير شراء، …) **منفصلة لكل دولة**.

**13.5 محاسبة (GL)**
- **`journal_vouchers.country_id`** (و/أو **`accounts.country_id`** للفصل القوي) — تقارير مالية **لا تخلط** دولتين.
- **ممنوع** قيد يجمع حساباً تشغيلياً من دولة أخرى.
- **قرار مالك (2026-05-20 — تأكيد بعد v55):** **دليل حسابات منفصل لكل دولة** — **`accounts.country_id`** إلزامي في التنفيذ المتبقي (**§15 — مرحلة 18**)؛ لا يكفي **`journal_vouchers.country_id`** وحده للفصل الكامل.

**13.6 حساب الواجهة (تسجيل البريد)**
- **فصل كامل:** **`UNIQUE (email, country_id)`** على `storefront_accounts` — نفس البريد **يُسمح** في دولتين **كحسابين منفصلين**.
- **يُلغي** سياسة **س11** («حساب واحد» عالمياً) **عند تفعيل** هذا البند في الكود — انظر تعديل **ORANGE_STOREFRONT_POLICY_REFERENCE.txt** تحت س11.

**13.7 الواجهة (متجر)**
- **الدولة تُثبت من رابط القناة** (`channels.country_id`) — العميل **لا يُلزَم** باختيار الدولة عند الدخول من رابط تسويق.
- **قائمة اختيار الدولة** في الشريط: تظهر عند **أكثر من دولة نشطة**؛ تُخفى عند **دولة واحدة** (مثل الكويت وحدها اليوم).

**13.8 الأدمن والصلاحيات**
- **أدمن عام:** `admin_users.country_id IS NULL` — مبدّل دولة في الهيدر؛ يرى حركات الدولة المختارة فقط في كل شاشة.
- **فريق دولة:** `admin_users.country_id` = دولته — **لا** يرى دولة أخرى.

**13.9 ترتيب التنفيذ البرمجي (بعد تثبيت هذا البند في الأرشيف)**
1. **مخازن + ترحيل كميات الكويت** (`warehouses`, `warehouse_variant_stock`, ترحيل v44+).
2. **`country_id`** على `orders`, `customers`, `suppliers`, `purchases`, `stock_movements`.
3. ربط **خصم/إضافة المخزون** (بيع، شراء، تعديل) بمخزن الدولة.
4. **`products.country_id`** + فلترة الكتalog.
5. **GL** scoped بالدولة + تقارير.
6. **`storefront_accounts`**: `country_id` + `UNIQUE(email, country_id)`.
7. **صلاحيات** `admin_users.country_id`.
8. **تفعيل دول جديدة** (مصر …) بنسخ قالب: مخزن افتراضي + قنوات + (لاحقاً) كتalog.

**تنفيذ (13 — مرحلة 1 — مخازن):** ترحيل **`orange_catalog_migrate_country_warehouses_v44`** في `includes/catalog_schema.php`؛ مساعدات **`includes/warehouses.php`**.

**تنفيذ (13 — مرحلة 2–3 — country_id + مخزن الدولة):** ترحيل **`orange_catalog_migrate_country_scope_v45`**؛ **`country_id`** على `orders`, `customers`, `suppliers`, `purchases`, `products`, `stock_movements` (+ `warehouse_id` على `orders`/`stock_movements`)؛ ربط حجز/إضافة المخزون عبر **`includes/warehouses.php`** في **`includes/order_stock.php`**, **`admin/api/stock/adjust.php`**, **`admin/api/purchases/create.php`**, **`includes/storefront_cart_items.php`**؛ سياق أدمن **`orange_admin_context_country_id`** (كوكي `orange_ad_country`) + مبدّل الدولة في **`admin/partials/header.php`**؛ فلترة قوائم **`orders`**, **`customers`**, **`suppliers`**.

**تنفيذ (13 — مرحلة 4–6 — منتجات + تسليم + حسابات واجهة):** ترحيل **`orange_catalog_migrate_country_accounts_v46`** (`storefront_accounts.country_id` + **`UNIQUE(email, country_id)`**؛ `admins.country_id`)؛ فلترة **`products.country_id`** في **`admin/pages/products.php`**, **`admin/api/products/create.php`**, **`api/products/get-products.php`**, **`pages/home.php`**, **`pages/product.php`**, **`admin/pages/purchases.php`**؛ خصم/إرجاع مخزن الدولة في **`includes/order_fulfillment.php`** و**`admin/api/purchases/update.php`**؛ تسجيل الواجهة scoped بالدولة في **`api/auth/request-email-verify.php`**؛ قفل فريق الدولة: **`admins.country_id`** → جلسة **`admin_country_lock`** + إخفاء مبدّل الدولة لغير الأدمن العام في **`admin/partials/header.php`**.

**تنفيذ (13 — مرحلة 5 — GL scoped):** ترحيل **`orange_catalog_migrate_country_gl_v47`** (`journal_vouchers.country_id`)؛ **`orange_gl_voucher_country_bind`** + **`orange_journal_voucher_stamp_country`** في **`includes/journal_voucher.php`**؛ فلترة تقارير/مجاميع GL؛ فلترة شاشات أدمن تشغيلية متبقية (**`dashboard`**, **`reports`**, **`stock`**, **`manual_order`**, **`sales_returns`**, **`purchase_returns`**, **`channel_analytics`**, شاشات الذمم**)؛ **`country_id`** على قيود تسليم الطلب عبر **`includes/order_fulfillment.php`** و**`includes/gl_pending_movements.php`**.

**تنفيذ (13 — مرحلة 7 — صلاحيات أدمن الدولة):** حقل **`admins.country_id`** في **`admin/api/admins/save.php`** و**`admin/api/admins/list.php`** و**`admin/pages/admin_users.php`** (NULL = أدمن عام؛ قيمة = فريق دولة)؛ يكمّل قفل الجلسة **`admin_country_lock`** المذكور في مرحلة 4–6.

**تنفيذ (13 — مرحلة 8 — تفعيل دولة + تهيئة تشغيلية):** **`orange_country_provision_operational`** في **`includes/countries.php`** (مخزن افتراضي + قناة ويب `{code}-web` idempotent)؛ يُستدعى من **`admin/api/countries/manage.php`** عند **`is_active=1`**؛ **`orange_country_copy_catalog_from_source`** في **`includes/country_catalog_copy.php`** (نسخ كتalog idempotent من الكويت؛ مخزون صفر).

**تنفيذ (13 — حماية API + تفرد ضمن الدولة):** **`orange_admin_assert_entity_country`** على مسارات التعديل/الحذف (**`orders/update`**, **`orders/update-status`**, **`orders/update-payment`**, **`customers/save`**, **`customers/delete`**, **`suppliers/save`**, **`products/update`**, **`products/delete`**, **`products/toggle`**, **`purchases/update`**, **`purchases/lines`**, مردودات المبيعات/المشتريات، شاشات الذمم، **`offers/save`**)؛ تفرد **`phone`/`code`/`civil_id`** (عملاء) و**`phone`/`code`/`tax_number`** (موردون) **ضمن `country_id`** في **`save`** APIs؛ فلترة موردي التقرير المالي في **`admin/pages/financial_report.php`**.

**تنفيذ (13 — تسلسلات ضمن الدولة):** **`orange_sequence_next(..., $countryId)`** في **`includes/document_sequences.php`**؛ **`orange_generate_order_number_for_country`** + بادئة كود الدولة في **`includes/order_helpers.php`**؛ فواتير **`sales_invoice`** وسندات الذمم scoped بالدولة.

**تنفيذ (13 — product_channels ضمن الدولة):** **`orange_product_attach_all_active_channels`** و**`orange_product_assert_channel_same_country`** في **`includes/product_channels.php`**.

**تنفيذ (13 — مرحلة 9 — ذمم ومردودات ومخزن):** فلترة **`orange_partner_summary_report`** و**`orange_partner_gl_reconcile`** بالدولة؛ **`assert`** على **`partners/aging`**, **`open-items`**, مرفقات العملاء/الموردين، **`sales_returns/create`**, **`purchase_returns/create`**, **`purchases/create`** (مورد)؛ أرقام مردود **`SR-{code}-`**, **`PR-{code}-`**؛ مخزن الدولة في **`sales_return_helpers`** و**`purchase_return_helpers`**.

**تنفيذ (13 — مرحلة 10 — طابور الطلبات + سندات المورد + backfill):** **`orange_order_intake_sql_country_scope`** + **`orange_admin_assert_order_intake_id`** في **`includes/order_intake_queue.php`**؛ فلترة شاشة/APIs الطابور (**`order_intake_queue`**, **`retry`**, **`delete`**, **`cleanup`**, **`retry-bulk-failed`**, **`export-csv`**, **`process-next`** scoped)؛ **`orange_gl_voucher_country_bind`** على **`partners/supplier-payment-list`** و**`supplier-payment-search`**؛ **`customers/backfill-orders`** + **`orange_ensure_customer(..., countryId)`** scoped بالدولة.

**تنفيذ (13 — مرحلة 11 — عروض السلة + قنوات + مردودات):** ترحيل **`orange_catalog_migrate_country_cart_promotions_v48`** (`country_id` على **`cart_promotions`**, **`cart_gift_promotions`**, **`cart_bogo_promotions`**, **`cart_combo_promotions`**)؛ **`includes/cart_promotion_country.php`** + فلترة/حفظ scoped في **`admin/api/cart_*_promotions/manage.php`** و**`includes/cart_*.php`**؛ فلترة **`admin/pages/offers.php`** و**`channels.php`**؛ **`orange_admin_assert_row_country`** على **`channels/toggle-active`**؛ assert على **`sales_returns/get`** و**`purchase_returns/get`**؛ فلترة قائمة **`sales_returns`** الأخيرة بالدولة.

**تنفيذ (13 — مرحلة 12 — assert تشغيلي):** **`channels/save`** (assert + قفل **`country_id`** لسياق الأدمن)؛ **`orders/create-manual`** (قناة + منتجات + عميل آجل)؛ **`delivery_areas/manage`** (assert على تعديل محافظة/منطقة)؛ **`sales_returns/update`** و**`purchase_returns/update`** (assert عميل/طلب/مورد/شراء + منتجات البنود).

**تنفيذ (13 — مرحلة 13 — سندات الذمم browse scoped):** **`admin/api/journal/manage.php`** — أنواع **`supplier_payment`** / **`customer_receipt`** في التنقل/البحث/العرض؛ alias **`search`→`search_manual`**؛ **`party_customer_id`** / **`party_supplier_id`** في **`get`**؛ **`orange_journal_voucher_assert_admin_context`** على **`get`**؛ **`orange_gl_voucher_country_bind`** على **`nav_manual`**؛ **`admin/api/partners/customer-receipt-list.php`** و**`customer-receipt-search.php`** (parity مع سندات المورد)؛ شاشة **`partner_party_voucher_ui.php`** (سداد العملاء) — تنقل/بحث/حذف scoped بالدولة.

**تنفيذ (13 — مرحلة 14 — مخزون فعلي §13.1):** **`orange_warehouse_effective_qty_sql`** في **`includes/warehouses.php`**؛ قراءة الرصيد عبر **`orange_warehouse_effective_variant_stock`** في **`api/cart/stock-limits.php`**, **`admin/api/orders/create-manual.php`**, **`admin/pages/manual_order.php`** (فلترة متغيرات الدولة)؛ عرض/تنبيه low-stock من **`warehouse_variant_stock`** في **`admin/pages/stock.php`**, **`admin/pages/dashboard.php`**, **`admin/api/reports/stats.php`**؛ **`orange_admin_assert_entity_country`** على **`admin/api/stock/adjust.php`**؛ فلترة طابور الطلبات في **`dashboard`**؛ **`orange_admin_assert_row_country`** اختياري على **`admin/api/channels/upload-logo.php`** عند تمرير **`channel_id`**.

---

14) ملخص ما تم + استراحة (قرار مالك — 2026-05-20)
---------------------------------------------------
**قرار مالك — دليل المحاسبة:** **منفصل لكل دولة** (`accounts.country_id`) — يُنفَّذ في **§15 مرحلة 18** (انظر **13.5**).

**Git (مرجع نشر):** تراكم **v44–v55** على **`main`** — آخر commit مرحلة 14: **`5572ec7`** (Country separation v55).

**ما تم — بند 13 (مراحل تنفيذ 1–14):**

| مرحلة | نطاق مختصر | ترحيل / مرجع |
|-------|------------|--------------|
| 1 | مخازن + `warehouses` / `warehouse_variant_stock` | v44 |
| 2–3 | `country_id` + مخزن الدولة على الطلبات/عملاء/موردين/مشتريات/حركات | v45 |
| 4–6 | منتجات scoped + `storefront_accounts` + `admins.country_id` + تسليم | v46 |
| 5 (GL) | `journal_vouchers.country_id` + تقارير/شاشات GL scoped | v47 |
| 7 | صلاحيات فريق الدولة (أدمن) | v46+ |
| 8 | تهيئة دولة (`provision` + نسخ كتalog) | `countries.php` / `country_catalog_copy.php` |
| حماية API | assert + تفرد phone/code ضمن الدولة | متفرق v48–v53 |
| 9 | ذمم + مردودات + مخزن مردود | v49–v50 |
| 10 | طابور طلبات + سندات مورد + backfill | v51 |
| 11 | عروض سلة + قنوات + مردودات get | v48, v52 |
| 12 | assert تشغيلي (قنوات، طلب يدوي، مناطق، مردودات update) | v53 |
| 13 | browse سندات ذمم (قبض/صرف) scoped | v54 |
| 14 | قراءة/عرض مخزون فعلي من المخزن | v55 |
| 15 | إغلاق مسارات مخزون §13.1 المتبقية | v56 |

**ما تم — خارج جدول §13 (جزئي من §10):** أساس **`countries`** + **`channels.country_id`** + سياق أدمن + محافظات/مناطق الكويت (§9–§10 مرحلة 1–2 جزئي).

**حالة (استراحة):** الفصل **التشغيلي الأساسي** للكويت **جاهز للتشغيل**؛ **مرحلة 15 منفّذة**؛ المتبقي **مراحل 16–19** للفصل الكامل. عند العودة: ابدأ **بالمرحلة 16** بالترتيب أدناه — **لا** تفترض اكتمال المشروع من Git وحده دون مطابقة هذه القائمة.

---

15) المراحل المتبقية (15–19) — استكمال فصل الدول الكامل
--------------------------------------------------------
**مصدر:** قرار مالك (2026-05-20) + مراجعة وكيل بعد v55. **الهدف:** إغلاق فجوات §13.1 و§13.5 و§13.7 وassert ثانوي، ثم **دليل حسابات منفصل**، ثم إنهاء المرحلة الانتقالية للمخزون.

**ترتيب التنفيذ الموصى عند العودة:** **15 → 16 → 17 → 18 → 19** (مرحلة 18 كبيرة — لا تبدأ قبل إغلاق 15–17 إلا بتوجيه صريح من المالك).

**مرحلة 15 — إغلاق مسارات المخزون §13.1 (متبقي):**
- **`admin/pages/item_card.php`**, **`admin/pages/opening_stock_balances.php`** — عرض/تعديل الرصيد من **`warehouse_variant_stock`** (مخزن الدولة) لا **`product_variants.stock_quantity`** مباشرة.
- **`includes/cart_gift_promotions.php`** (و/أو APIs الهدايا) — فحص مخزون الهدية scoped بالدولة.
- **`includes/product_variants_write.php`**, **`admin/api/settings/variants_search.php`**, **`admin/assets/admin-variant-picker.js`** — قراءة/كتابة متسقة مع المخزن.
- **`admin/api/products/get.php`** / شاشة المنتجات — عرض رصيد effective حيث يلزم.
- **معيار إنجاز:** لا مسار بيع/شراء/تعديل/عرض **حساس** يعتمد على **`stock_quantity`** وحده خارج مزامنة الكويت الانتقالية.

**تنفيذ (13 — مرحلة 15 — مخزون §13.1 متبقي):** **`item_card.php`**, **`opening_stock_balances.php`** — رصيد effective + assert منتج؛ **`cart_gift_promotions.php`** — **`orange_warehouse_effective_variant_stock`** + **`countryId`** اختياري؛ **`product_variants_write.php`** — تصفير/إنشاء عبر المخزن؛ **`variants_search.php`**, **`products/get.php`** — فلترة دولة + رصيد effective؛ **`admin-variant-picker.js`** يستهلك **`variants_search`** (لا تغيير JS).

**مرحلة 16 — مراجعة assert + شاشات/APIs ثانوية:**
- مسح **`admin/api/**`** و**`admin/pages/**`** لمسارات بلا **`orange_admin_assert_*`** أو **`orange_sql_country_*`** حيث يُعدَّل/يُعرض كيان scoped.
- أمثلة مراجعة: **`variants_search`**, **`item_card`**, **`opening_stock_balances`**, **`channel_analytics`**, مرفقات/طباعة بطاقات، أي تقرير فرعي في **`admin/api/reports/**`**.
- **معيار إنجاز:** قائمة مسارات مُراجَعة + assert أو فلترة موثّقة؛ لا cross-country write.

**مرحلة 17 — واجهة §13.7 + سياسة s11 (متعدد الدول نشط):**
- **§13.7:** قائمة اختيار الدولة في الشريط عند **>1 دولة `is_active`**؛ إخفاؤها عند دولة واحدة؛ الدولة من **`channels.country_id`** عند الدخول برابط قناة.
- **`ORANGE_STOREFRONT_POLICY_REFERENCE.txt`:** التأكد أن **س11** و**«تنفيذ»** متسقة مع **`UNIQUE(email, country_id)`** (v46) — مراجعة نص «حساب واحد» القديم إن بقي مُربكاً.
- **معيار إنجاز:** سيناريو دولتين نشطتين في الواجهة + تسجيل بريد منفصل per country يعمل.

**مرحلة 18 — دليل حسابات منفصل (`accounts.country_id`) — §13.5 (قرار مالك):**
- ترحيل **`accounts.country_id`** (+ فهارس؛ ربط حسابات الكويت الحالية بـ **`country_id` الكويت**).
- **`orange_gl_account_*`** / **`admin/pages/accounts`**, **`admin/api/accounts/**`**, **`includes/gl_settings.php`**, **`includes/account_tree.php`** — فلترة وإنشاء scoped؛ منع قيد يستخدم حساباً من دولة أخرى (مع **`journal_vouchers.country_id`**).
- **`orange_gl_account_id_optional`** / إعدادات القيود التلقائية — **نسخة لكل دولة** أو ربط mapping per country.
- تقارير GL (**`financial_report`**, **`reports`**, **`dashboard`**) — لا خلط حسابات دولتين.
- **معيار إنجاز:** شجرتا دليل (أو أكثر) معزولتان؛ قيد + تقرير في سياق دولة واحدة فقط.

**مرحلة 19 — إنهاء المرحلة الانتقالية للمخزون §13.1:**
- **`warehouse_variant_stock`** = **مصدر الحقيقة الوحيد** داخل كل دولة.
- إيقاف/تضييق **`orange_warehouse_sync_legacy_variant_quantity`** (مزامنة **`product_variants.stock_quantity`** مع مخزن الكويت فقط) بعد اكتمال مرحلة 15.
- توثيق: **`product_variants.stock_quantity`** للعرض legacy أو **deprecated** — حسب قرار المالك عند التنفيذ.
- **معيار إنجاز:** تعديل مخزون في مصر (مثلاً) **لا** يغيّر **`stock_quantity`** على متغير كتalog الكويت.

**بعد 15–19 (تشغيل — ليس «مرحلة كود» إلزامية):** تفعيل دول جديدة (§10 مرحلة 5): مصر → الإمارات → السعودية عبر **`admin/api/countries/manage.php`** + **`orange_country_provision_operational`** + **`orange_country_copy_catalog_from_source`**.

**خارج نطاق 15–19 (مؤجّل برؤية الملف):** عملة لكل دولة (§3)؛ دفع إلكتروني (§7)؛ كتalog موحّد ضخم — **`ORANGE_UNIFIED_TAXONOMY_AND_CATALOG_ERD.txt`**.

نهاية الملف.
